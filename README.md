# Anvil Uncap

Removes the enchantment level cap when combining items in an anvil. Requires [Fabric API](https://www.curseforge.com/minecraft/mc-mods/fabric-api).

## Configuration

`config/anviluncap.json`

- `maxCombinedLevel`: max level allowed after combining. `0` or lower is uncapped (default).
